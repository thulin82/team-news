{{> header }}
{{#each squad.players}}
<li>
    <strong>{{ player.shirtNumber }} - {{ player.name }} - {{ player.position }}</strong>
    Contract until: {{ prettifyDate player.contractUntilTimestamp }}
</li>
{{/each}}
{{> footer parameter='js/squad.js'}}